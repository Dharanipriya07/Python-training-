#include<stdio.h>
int main()
{
    int n,i;
    int sum=0;
    scanf("%d",&n);
    while(n!=0)
    {
       sum=sum+(n%10);
       n=n/10;
    }
    printf("The sum of the digit is %d",sum);
    return 0;
}